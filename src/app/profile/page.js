import React from "react";

function ProfilePage() {
  return (
    <div>
      <h1>Profile</h1>
      {/* Would it be easier to create a pool of default profile pictures or add the ability to upload an image? We'd have to have something on the admin side to stop people using nudes or something for profile pictures. */}
    </div>
  );
}

export default ProfilePage;
