import React from "react";

function SearchPage() {
  return (
    <div>
      <h1>List Searcher</h1>
      {/* What types of sorting are we using? Alphabetical? Size of list? Presumably we're not going to let people make lists of one, because that's not a list. */}
    </div>
  );
}

export default SearchPage;
